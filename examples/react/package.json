{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "devDependencies": {
    "snowpack": "^3.0.1"
  },
  "dependencies": {
    "@harnessio/ff-javascript-client-sdk": "file:///../..",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
